/**
 * @fileOverview
 * @author daiying.zhang
 */

require.config({
    // 相对于引入main.js的文件的路径
    baseUrl: '../src',
    // 相对于baseUrl
    paths: {
        lib : 'lib',
        scripts: 'scripts'
    }
});